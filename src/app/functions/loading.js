const $ = require('jquery');

export function loading(status) {
	// if (status) {
	//     Pace.start({
	//         elements: true
	//     });
	//     console.log(Pace);
	// } else {
	//     Pace.stop();
	// }
}